You do not need to install anything (other than xmlroff, that is) to
be able to use these examples.

To build:

   ./configure
   make -k

(or './autogen.sh' instead of './configure' when building from a
Subversion checkout).
